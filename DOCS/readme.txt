The server can be run as is.
All configs are in files named "application.yml"

The Client expectes a password to be given as an argument (this password will be used to encrpyt private data saved on the server)